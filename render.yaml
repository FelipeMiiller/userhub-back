services:
  - type: web
    name: conectar-userhub-api
    runtime: node
    buildCommand: yarn install && yarn build
    startCommand: node ./dist/src/main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000

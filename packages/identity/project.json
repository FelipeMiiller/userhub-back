{
  "name": "identity",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/identity",
  "projectType": "library",
  "tags": [],
  "targets": {
    "lint:check": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/identity/**/*.ts"]
      }
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/identity/**/*.ts"],
        "fix": true
      }
    },
    "test:unit": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/identity/jest.config.ts",
        "passWithNoTests": true,
        "testMatch": ["<rootDir>/**/unit/**/*.spec.ts"]
      }
    },
    "test:e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/identity/jest.config.ts",
        "passWithNoTests": true,
        "testMatch": ["<rootDir>/**/e2e/**/*.spec.ts"],
        "runInBand": true
      }
    },
    "db:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node -r tsconfig-paths/register -P {projectRoot}/tsconfig.lib.json ./node_modules/typeorm/cli.js migration:generate {projectRoot}/persistence/migrations/Migration -d {projectRoot}/persistence/typeorm-datasource.ts",
        "cwd": "."
      }
    },
    "db:migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node -r tsconfig-paths/register -P {projectRoot}/tsconfig.lib.json ./node_modules/typeorm/cli.js migration:run -d {projectRoot}/persistence/typeorm-datasource.ts",
        "cwd": "."
      }
    },
    "db:drop": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node -r tsconfig-paths/register -P {projectRoot}/tsconfig.lib.json ./node_modules/typeorm/cli.js schema:drop -d {projectRoot}/persistence/typeorm-datasource.ts",
        "cwd": "."
      }
    }
  }
}
